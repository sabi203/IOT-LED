void setup(){
 serial.begin(115200);
 pinmode (19,OUTPUT);
 pinmode (5,OUTPUT);
 pinmode (34,INPUT);
}
void loop(){
 if(digitalread(34)==HIGH)
{
 digitalwrite(19,HIGH);
 digitalwrite(5,LOW);
}
else
{
  digitalwrite(19,LOW);
  digitalwrite(5,HIGH);
}
}
